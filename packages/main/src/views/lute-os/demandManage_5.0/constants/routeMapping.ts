import type { DemandWorkbenchQueryState } from '../types/workbench';

export const defaultWorkbenchQuery: DemandWorkbenchQueryState = {
  view: 'list',
  workTab: 'list',
  bizTab: 'all',
  demandCode: '',
};
